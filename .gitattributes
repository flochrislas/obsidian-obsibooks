# Normalize all text files to LF in the repo.
# Working tree stays LF too — no CRLF round-trip on Windows.
* text=auto eol=lf

# Binary files: never touch
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
